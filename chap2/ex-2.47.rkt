#lang racket

; Exercise 2.47

(define (origin-frame f)
  (car f))

(define (origin-frame f)
  (car f))

(define (edge1-frame f)
  (cadr f))

(define (edge1-frame f)
  (cadr f))

(define (edge2-frame f)
  (caddr f))

(define (edge2-frame f)
  (cddr f))

